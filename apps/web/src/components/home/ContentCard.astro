---
import type { RecentContentItem } from "../../data/recentContent";

interface Props {
  item: RecentContentItem;
}

const { item } = Astro.props;
const isAvailable = item.status === "Disponible" && item.href;
---

{
  isAvailable ? (
    <a class="content-card card content-card-link" href={item.href}>
      <img
        src={item.image}
        alt={item.alt}
        width="640"
        height="420"
        loading="lazy"
        decoding="async"
      />

      <div class="content-card-body">
        <div class="content-card-top">
          <span class="format-badge">{item.format}</span>
          <span class="content-category">{item.category}</span>
        </div>

        <h3>{item.title}</h3>

        <dl class="content-meta">
          <div>
            <dt>Nivel</dt>
            <dd>{item.level}</dd>
          </div>

          <div>
            <dt>Duración</dt>
            <dd>{item.duration}</dd>
          </div>
        </dl>

        <span class="content-action">Ver contenido</span>
      </div>
    </a>
  ) : (
    <article class="content-card card">
      <img
        src={item.image}
        alt={item.alt}
        width="640"
        height="420"
        loading="lazy"
        decoding="async"
      />

      <div class="content-card-body">
        <div class="content-card-top">
          <span class="format-badge">{item.format}</span>
          <span class="content-category">{item.category}</span>
        </div>

        <h3>{item.title}</h3>

        <dl class="content-meta">
          <div>
            <dt>Nivel</dt>
            <dd>{item.level}</dd>
          </div>

          <div>
            <dt>Duración</dt>
            <dd>{item.duration}</dd>
          </div>
        </dl>

        <div class="content-action" aria-label="Contenido demostrativo sin enlace disponible todavía">
          <span>{item.status}</span>
          <span class="content-path">Sin enlace disponible todavía</span>
        </div>
      </div>
    </article>
  )
}

<style>
  .content-card {
    display: grid;
    grid-template-rows: auto 1fr;
    min-height: 100%;
    overflow: hidden;
    transition:
      transform 180ms ease,
      border-color 180ms ease,
      box-shadow 180ms ease;
  }

  .content-card-link:focus-visible {
    outline: 3px solid var(--color-gold);
    outline-offset: 4px;
  }

  .content-card-link:hover {
    transform: translateY(-3px);
    border-color: rgba(0, 85, 164, 0.3);
    box-shadow: var(--shadow-md);
  }

  .content-card img {
    width: 100%;
    aspect-ratio: 16 / 10.5;
    height: auto;
    object-fit: cover;
    background: var(--color-surface-soft);
  }

  .content-card-body {
    display: grid;
    grid-template-rows: auto auto 1fr auto;
    padding: 1rem;
  }

  .content-card-top {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: space-between;
    gap: 0.5rem;
  }

  .format-badge {
    display: inline-flex;
    min-height: 1.7rem;
    align-items: center;
    padding: 0.25rem 0.58rem;
    border-radius: 999px;
    background: var(--color-blue-dark);
    color: #ffffff;
    font-size: 0.68rem;
    font-weight: 800;
    letter-spacing: 0.04em;
    text-transform: uppercase;
  }

  .content-category {
    color: var(--color-text-muted);
    font-size: 0.72rem;
    font-weight: 700;
  }

  .content-card h3 {
    margin-top: 1rem;
    font-size: 1.12rem;
  }

  .content-meta {
    align-self: end;
    display: flex;
    justify-content: space-between;
    gap: 1rem;
    margin: 1.25rem 0 0;
  }

  .content-meta div {
    display: grid;
    gap: 0.12rem;
  }

  .content-meta dt {
    color: var(--color-text-muted);
    font-size: 0.68rem;
    font-weight: 800;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .content-meta dd {
    margin: 0;
    color: var(--color-heading);
    font-size: 0.82rem;
    font-weight: 800;
  }

  .content-action {
    display: grid;
    gap: 0.15rem;
    min-height: 2.75rem;
    align-content: end;
    margin-top: 1rem;
    color: var(--color-blue);
    font-size: 0.78rem;
    font-weight: 800;
  }

  .content-path {
    color: var(--color-text-muted);
    font-size: 0.68rem;
    font-weight: 700;
  }

  @media (prefers-reduced-motion: reduce) {
    .content-card {
      transition: none;
    }

    .content-card-link:hover {
      transform: none;
    }
  }
</style>
